# Spring Boot API 处理时间计算 - 使用说明

## 项目简介

这是一个Spring Boot应用程序，演示如何计算后端接口的处理时间。项目提供了两种计算方式：

1. **拦截器方式** - 使用HandlerInterceptor计算API请求处理时间
2. **AOP方式** - 使用@Timing注解和AOP切面计算方法执行时间

## 快速开始

### 1. 环境要求

- Java 11 或更高版本
- Maven 3.6 或更高版本

### 2. 启动应用程序

```bash
# 方式1：使用启动脚本（推荐）
./start_app.sh

# 方式2：直接使用Maven命令
mvn spring-boot:run

# 方式3：编译后运行
mvn clean package
java -jar target/springboot-api-timing-1.0.0.jar
```

### 3. 测试接口

应用程序启动后，可以通过以下方式测试：

#### 使用测试脚本（推荐）
```bash
# 在另一个终端窗口中运行
./test_api_timing.sh
```

#### 手动测试
```bash
# 快速响应接口
curl http://localhost:8080/api/quick

# 模拟处理时间接口
curl "http://localhost:8080/api/process?delay=1000"

# 数据库查询模拟接口
curl "http://localhost:8080/api/database?records=10"

# 文件处理模拟接口
curl -X POST "http://localhost:8080/api/file?filename=test.txt&size=1024"

# 复杂业务逻辑模拟接口
curl "http://localhost:8080/api/complex?steps=3"

# 健康检查接口
curl http://localhost:8080/api/health
```

## 功能详解

### 1. 拦截器方式

#### 工作原理
- 在请求进入控制器前记录开始时间
- 在请求处理完成后计算处理时间
- 将处理时间记录到日志和响应头中

#### 配置
```java
@Configuration
public class WebConfig implements WebMvcConfigurer {
    @Override
    public void addInterceptors(InterceptorRegistry registry) {
        registry.addInterceptor(apiTimingInterceptor)
                .addPathPatterns("/api/**")  // 只拦截 /api/** 路径
                .excludePathPatterns("/api/health"); // 排除健康检查
    }
}
```

#### 日志输出示例
```
2024-01-01 12:00:00.000 [http-nio-8080-exec-1] INFO  c.e.s.i.ApiTimingInterceptor - API请求开始: GET /api/quick
2024-01-01 12:00:00.100 [http-nio-8080-exec-1] INFO  c.e.s.i.ApiTimingInterceptor - API请求完成: GET /api/quick - 处理时间: 100ms, 状态码: 200
```

#### 响应头
```
X-Processing-Time: 100
X-Processing-Time-Unit: ms
```

### 2. AOP方式

#### 工作原理
- 使用@Timing注解标记需要计算执行时间的方法
- 通过AOP切面在方法执行前后记录时间
- 计算并记录方法执行时间

#### 使用示例
```java
@Service
public class BusinessService {
    @Timing("快速业务处理")
    public Map<String, Object> quickBusiness() {
        // 业务逻辑
        return result;
    }
}
```

#### 日志输出示例
```
2024-01-01 12:00:00.000 [http-nio-8080-exec-1] INFO  c.e.s.a.TimingAspect - 方法执行完成: 快速业务处理 - 执行时间: 50ms
```

## 测试接口说明

### 基础API接口（使用拦截器）

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 快速响应 | GET | `/api/quick` | 快速响应，无延迟 |
| 模拟处理 | GET | `/api/process` | 可设置延迟时间 |
| 数据库查询 | GET | `/api/database` | 模拟数据库查询时间 |
| 文件处理 | POST | `/api/file` | 模拟文件处理时间 |
| 复杂业务 | GET | `/api/complex` | 模拟复杂业务逻辑 |
| 健康检查 | GET | `/api/health` | 健康检查（不计算时间） |

### 服务接口（使用AOP注解）

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 快速业务 | GET | `/api/service/quick` | 快速业务处理 |
| 慢速业务 | GET | `/api/service/slow` | 慢速业务处理（2秒延迟） |
| 数据库操作 | GET | `/api/service/database` | 数据库操作模拟 |
| 文件处理 | POST | `/api/service/file` | 文件处理模拟 |
| 复杂计算 | GET | `/api/service/calculation` | 复杂计算模拟 |

## 配置说明

### 应用配置 (application.yml)

```yaml
server:
  port: 8080  # 服务端口

logging:
  level:
    com.example.springbootapitiming: INFO  # 应用日志级别
    org.springframework.web: DEBUG         # Web日志级别

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics  # 暴露的监控端点
```

### 拦截器配置

可以通过修改`WebConfig.java`中的配置来调整拦截器行为：

```java
@Override
public void addInterceptors(InterceptorRegistry registry) {
    registry.addInterceptor(apiTimingInterceptor)
            .addPathPatterns("/api/**")           // 拦截路径
            .excludePathPatterns("/api/health");  // 排除路径
}
```

## 监控和调试

### 1. 查看日志

应用程序会输出详细的处理时间日志，包括：
- 请求开始时间
- 请求完成时间
- 处理时间
- HTTP状态码

### 2. 查看响应头

每个API响应都包含处理时间信息：
- `X-Processing-Time`: 处理时间（毫秒）
- `X-Processing-Time-Unit`: 时间单位

### 3. 使用监控端点

Spring Boot Actuator提供了监控端点：
- `/actuator/health` - 健康检查
- `/actuator/info` - 应用信息
- `/actuator/metrics` - 指标信息

## 扩展功能

### 1. 集成监控系统

可以将处理时间数据发送到监控系统：

```java
@Component
public class MetricsService {
    @Autowired
    private MeterRegistry meterRegistry;
    
    public void recordProcessingTime(String endpoint, long time) {
        Timer.Sample sample = Timer.start(meterRegistry);
        sample.stop(Timer.builder("api.processing.time")
                .tag("endpoint", endpoint)
                .register(meterRegistry));
    }
}
```

### 2. 数据库存储

可以将处理时间数据存储到数据库中：

```java
@Entity
public class ApiTimingRecord {
    private String endpoint;
    private long processingTime;
    private LocalDateTime timestamp;
    // getters and setters
}
```

### 3. 告警机制

可以设置处理时间阈值：

```java
@Component
public class AlertService {
    private static final long THRESHOLD = 5000; // 5秒阈值
    
    public void checkProcessingTime(String endpoint, long time) {
        if (time > THRESHOLD) {
            // 发送告警
            sendAlert(endpoint, time);
        }
    }
}
```

## 常见问题

### Q: 如何处理异步请求的处理时间计算？

A: 对于异步请求，需要在异步处理完成后手动记录时间，或者使用CompletableFuture的回调机制。

### Q: 如何避免拦截器影响性能？

A: 可以通过以下方式优化：
1. 使用异步日志记录
2. 设置采样率，只记录部分请求
3. 使用更高效的日志框架

### Q: 如何自定义处理时间的计算逻辑？

A: 可以继承`ApiTimingInterceptor`类并重写相关方法，或者创建自定义的拦截器。

## 总结

这个Spring Boot应用程序提供了完整的API处理时间计算功能，包括：

1. **两种计算方式** - 拦截器和AOP，满足不同场景需求
2. **丰富的测试接口** - 模拟各种业务场景
3. **详细的日志记录** - 便于调试和监控
4. **灵活的配置** - 可以根据需要调整拦截规则
5. **扩展性强** - 可以轻松集成到现有系统中

通过这个项目，你可以学习到Spring Boot中处理时间计算的最佳实践，并将其应用到实际项目中。