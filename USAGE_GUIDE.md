# 直播间管理系统 - 使用指南

## 快速开始

### 1. 启动系统
```bash
# 方法1: 使用启动脚本（推荐）
./start.sh

# 方法2: 手动启动
python3 run.py --init-db
```

### 2. 访问系统
打开浏览器访问: `http://localhost:5000`

## 功能使用说明

### 预约管理

#### 创建预约
1. 点击"新建预约"按钮
2. 填写预约信息：
   - **选择房间**: 从下拉列表选择可用房间
   - **用户姓名**: 输入预约人姓名
   - **手机号**: 输入预约人手机号（用于门禁验证）
   - **开始时间**: 选择预约开始时间
   - **结束时间**: 选择预约结束时间
3. 点击"创建预约"完成

#### 预约状态管理
- **待开始**: 预约已创建，等待开始时间到达
- **进行中**: 预约时间已到，可以正常使用
- **已完成**: 预约时间结束，使用完成
- **已取消**: 预约被取消

#### 预约操作
- **开始**: 手动开始预约（提前开始）
- **结束**: 手动结束预约（提前结束）
- **取消**: 取消预约

### 门禁控制

#### 自动控制
- 系统会根据预约时间自动控制门禁
- 预约时间到达时自动解锁
- 预约时间结束后自动锁定

#### 手动控制
- **解锁**: 点击房间卡片上的"解锁"按钮
- **锁定**: 点击房间卡片上的"锁定"按钮
- **门禁验证**: 点击"门禁"按钮，输入手机号验证访问权限

#### 门禁状态
- **已锁定**: 门禁处于锁定状态，无法进入
- **已解锁**: 门禁处于解锁状态，可以进入

### 使用时长监控

#### 实时监控
- 系统会实时记录实际使用时间
- 显示在预约列表中
- 自动计算使用时长

#### 超时处理
- 预约时间结束后，系统会等待一段时间再自动锁定
- 防止用户忘记结束预约

### 访问日志

#### 日志类型
- **解锁**: 门禁被解锁
- **锁定**: 门禁被锁定
- **允许访问**: 用户通过验证成功进入
- **拒绝访问**: 用户验证失败

#### 日志查看
- 在"访问日志"区域查看所有操作记录
- 按时间倒序显示
- 包含操作时间、房间、操作类型、原因等信息

## 使用场景示例

### 场景1: 正常预约使用
1. 张三预约明天上午9:00-11:00使用直播间1
2. 系统在9:00自动解锁门禁
3. 张三到达后，输入手机号验证身份
4. 验证通过，张三进入直播间
5. 11:00预约时间结束，系统自动锁定门禁

### 场景2: 提前结束使用
1. 李四预约今天下午2:00-4:00使用直播间2
2. 李四在3:30提前完成使用
3. 管理员点击"结束"按钮提前结束预约
4. 系统立即锁定门禁

### 场景3: 紧急情况
1. 王五正在使用直播间3
2. 发生紧急情况需要立即锁定门禁
3. 管理员点击"锁定"按钮强制锁定
4. 系统记录紧急锁定操作

## 系统配置

### 时间配置
- **门禁解锁持续时间**: 30秒（可配置）
- **自动锁门延迟**: 5分钟（可配置）
- **定时检查间隔**: 30秒（可配置）

### 预约配置
- **最大预约时长**: 8小时（可配置）
- **最小预约时长**: 30分钟（可配置）
- **可提前预约天数**: 7天（可配置）

### 硬件配置
- **硬件模拟**: 默认启用（开发环境）
- **硬件端口**: 8080（可配置）

## 故障排除

### 常见问题

#### 1. 无法访问系统
- 检查系统是否正在运行
- 确认端口5000未被占用
- 检查防火墙设置

#### 2. 门禁无法解锁
- 检查预约时间是否正确
- 确认预约状态为"进行中"
- 检查硬件连接

#### 3. 预约创建失败
- 检查时间是否冲突
- 确认开始时间晚于当前时间
- 检查结束时间晚于开始时间

#### 4. 数据库错误
- 删除数据库文件重新初始化
- 检查磁盘空间
- 检查文件权限

### 日志查看
```bash
# 查看系统日志
tail -f livestream_management.log

# 查看错误日志
grep "ERROR" livestream_management.log
```

### 系统重启
```bash
# 停止系统
pkill -f "python3 run.py"

# 重新启动
python3 run.py --init-db
```

## 高级功能

### API接口使用
系统提供完整的RESTful API接口，可以集成到其他系统：

```bash
# 获取房间列表
curl http://localhost:5000/api/rooms

# 创建预约
curl -X POST http://localhost:5000/api/bookings \
  -H "Content-Type: application/json" \
  -d '{"room_id":1,"user_name":"张三","user_phone":"13800138000","start_time":"2024-01-01T09:00:00","end_time":"2024-01-01T11:00:00"}'

# 验证访问权限
curl -X POST http://localhost:5000/api/access/1 \
  -H "Content-Type: application/json" \
  -d '{"user_phone":"13800138000"}'
```

### 硬件集成
系统支持集成真实门禁硬件：

1. 修改`hardware_simulator.py`中的接口
2. 实现真实的硬件控制函数
3. 配置硬件连接参数

### 数据备份
```bash
# 备份数据库
cp livestream_management.db backup_$(date +%Y%m%d).db

# 恢复数据库
cp backup_20240101.db livestream_management.db
```

## 技术支持

### 系统要求
- Python 3.7+
- 内存: 最少512MB
- 磁盘: 最少100MB可用空间
- 网络: 本地网络访问

### 性能优化
- 定期清理历史日志
- 优化数据库查询
- 增加系统内存
- 使用SSD存储

### 安全建议
- 定期更新系统
- 设置访问密码
- 启用HTTPS
- 定期备份数据