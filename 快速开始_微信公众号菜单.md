# 微信公众号菜单系统 - 快速开始

## 🚀 5分钟快速上手

### 第一步：安装依赖

```bash
pip install flask requests
```

### 第二步：配置公众号信息

编辑 `wechat_official_account_config.ini`：

```ini
[WECHAT_OFFICIAL]
appid = 你的AppID
appsecret = 你的AppSecret
token = 你的Token
```

**获取方式：**
- 正式公众号：登录 [微信公众平台](https://mp.weixin.qq.com/) → 开发 → 基本配置
- 测试公众号（推荐）：访问 [测试号申请](https://mp.weixin.qq.com/debug/cgi-bin/sandbox?t=sandbox/login)

### 第三步：创建菜单

```bash
# 方式1: 使用脚本（最简单）
./start_wechat_official.sh

# 方式2: 直接运行
python3 wechat_official_menu.py
```

### 第四步：启动服务器

```bash
python3 wechat_official_server.py
```

### 第五步：配置微信服务器

在微信公众平台配置：
- URL: `http://你的域名:8080/wechat`
- Token: 与配置文件一致
- 消息加解密方式: 明文模式

## 🎯 创建第一个菜单

### 示例1：简单菜单

```python
from wechat_official_menu import WeChatOfficialMenuManager

manager = WeChatOfficialMenuManager()

menu = {
    "button": [
        {
            "type": "click",
            "name": "点我试试",
            "key": "TEST_BUTTON"
        }
    ]
}

manager.create_menu(menu)
```

### 示例2：带图片响应的菜单

```python
# 1. 准备图片
# 将图片放到 media/images/my_image.jpg

# 2. 创建菜单
menu = {
    "button": [
        {
            "type": "click",
            "name": "查看图片",
            "key": "MY_IMAGE"
        }
    ]
}
manager.create_menu(menu)

# 3. 在 wechat_official_server.py 中添加响应处理
# 找到 handle_menu_click 函数，添加：
if event_key == "MY_IMAGE":
    media_id = manager.upload_media('./media/images/my_image.jpg', 'image')
    return build_image_response(to_user, from_user, media_id)
```

## 📱 完整示例：多媒体菜单

```python
menu = {
    "button": [
        {
            "name": "多媒体",
            "sub_button": [
                {
                    "type": "click",
                    "name": "📸 图片",
                    "key": "SHOW_IMAGE"
                },
                {
                    "type": "click",
                    "name": "🎬 视频",
                    "key": "SHOW_VIDEO"
                },
                {
                    "type": "click",
                    "name": "🎵 音频",
                    "key": "SHOW_AUDIO"
                }
            ]
        },
        {
            "type": "click",
            "name": "关于",
            "key": "ABOUT"
        }
    ]
}

manager.create_menu(menu)
```

## 🎨 自定义响应

在 `wechat_official_server.py` 的 `handle_menu_click` 函数中：

```python
def handle_menu_click(event_key, to_user, from_user):
    if event_key == "YOUR_KEY":
        # 返回文本
        return build_text_response(to_user, from_user, "你好！")
    
    elif event_key == "IMAGE_KEY":
        # 返回图片
        media_id = manager.upload_media('path/to/image.jpg', 'image')
        return build_image_response(to_user, from_user, media_id)
    
    elif event_key == "VIDEO_KEY":
        # 返回视频
        media_id = manager.upload_media('path/to/video.mp4', 'video')
        return build_video_response(to_user, from_user, media_id)
```

## 🔧 常用命令

```bash
# 创建菜单
python3 wechat_official_menu.py

# 查询菜单
python3 -c "from wechat_official_menu import WeChatOfficialMenuManager; m = WeChatOfficialMenuManager(); print(m.get_menu())"

# 删除菜单
python3 -c "from wechat_official_menu import WeChatOfficialMenuManager; m = WeChatOfficialMenuManager(); m.delete_menu()"

# 上传素材
python3 -c "from wechat_official_menu import WeChatOfficialMenuManager; m = WeChatOfficialMenuManager(); print(m.upload_media('image.jpg', 'image'))"
```

## ⚠️ 常见问题

**Q: 菜单不显示？**
- 取消关注后重新关注
- 等待24小时生效

**Q: 点击菜单没反应？**
- 检查服务器是否运行
- 查看服务器日志

**Q: 图片/视频上传失败？**
- 检查文件大小和格式
- 确认文件路径正确

## 📖 进阶学习

查看完整文档：`README_微信公众号菜单.md`

运行完整示例：
```bash
python3 wechat_official_example.py
```

## 💡 提示

1. 开发时使用测试公众号
2. 媒体文件提前准备好
3. 定期查看日志文件
4. 服务器需要公网IP

---

**开始创建您的菜单吧！** 🎉
