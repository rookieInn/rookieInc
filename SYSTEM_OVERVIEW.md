# 直播间管理系统 - 系统概览

## 系统架构图

```
┌─────────────────────────────────────────────────────────────┐
│                    前端界面层                                │
├─────────────────────────────────────────────────────────────┤
│  Web管理界面  │  房间状态显示  │  预约管理  │  门禁控制  │
│  (HTML/CSS/JS) │  (实时更新)   │  (CRUD)   │  (开关控制) │
└─────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────┐
│                    后端服务层                                │
├─────────────────────────────────────────────────────────────┤
│  Flask Web服务器  │  API接口层  │  业务逻辑层  │  数据访问层  │
│  (HTTP服务)      │  (RESTful)  │  (核心逻辑)  │  (ORM)      │
└─────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────┐
│                    数据存储层                                │
├─────────────────────────────────────────────────────────────┤
│  SQLite数据库  │  房间表  │  预约表  │  访问日志表  │
│  (持久化存储)   │  (Room)  │ (Booking)│ (AccessLog) │
└─────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────┐
│                    门禁控制层                                │
├─────────────────────────────────────────────────────────────┤
│  门禁控制器  │  硬件模拟器  │  定时任务调度器  │  状态监控  │
│  (DoorController)│(HardwareSim)│  (Scheduler)   │(StatusMonitor)│
└─────────────────────────────────────────────────────────────┘
```

## 核心功能模块

### 1. 预约管理模块
- **功能**: 创建、查看、修改、取消预约
- **特性**: 
  - 时间冲突检测
  - 预约状态管理（待开始、进行中、已完成、已取消）
  - 实际使用时间记录

### 2. 门禁控制模块
- **功能**: 基于预约时间自动控制门禁开关
- **特性**:
  - 预约时间到达自动解锁
  - 预约结束自动锁定
  - 手动门禁控制
  - 访问权限验证

### 3. 使用时长监控模块
- **功能**: 实时监控直播间使用情况
- **特性**:
  - 实际开始时间记录
  - 实际结束时间记录
  - 使用时长统计
  - 超时自动提醒

### 4. 访问日志模块
- **功能**: 记录所有门禁操作和访问记录
- **特性**:
  - 操作类型记录（解锁、锁定、访问成功、访问拒绝）
  - 时间戳记录
  - 操作原因记录
  - 历史查询功能

## 数据流程

### 预约创建流程
```
用户填写预约信息 → 时间冲突检查 → 创建预约记录 → 返回预约ID
```

### 门禁控制流程
```
定时任务检查 → 预约时间到达 → 自动解锁门禁 → 记录操作日志
```

### 访问验证流程
```
用户输入手机号 → 查找有效预约 → 验证时间范围 → 允许/拒绝访问
```

### 自动锁门流程
```
预约时间结束 → 延迟等待 → 自动锁定门禁 → 更新预约状态
```

## 技术特性

### 前端技术
- **响应式设计**: 支持桌面和移动设备
- **实时更新**: 30秒自动刷新状态
- **用户友好**: 直观的操作界面
- **状态指示**: 清晰的门禁状态显示

### 后端技术
- **RESTful API**: 标准化的接口设计
- **定时任务**: 自动处理预约时间验证
- **数据验证**: 完整的数据校验机制
- **错误处理**: 完善的异常处理

### 数据库设计
- **关系型设计**: 规范化的数据表结构
- **索引优化**: 提高查询性能
- **数据完整性**: 外键约束保证数据一致性
- **审计日志**: 完整的操作记录

### 硬件接口
- **模拟器支持**: 内置硬件模拟功能
- **接口抽象**: 便于集成真实硬件
- **状态监控**: 实时硬件状态反馈
- **故障处理**: 异常情况处理机制

## 安全特性

### 访问控制
- **时间验证**: 严格的时间范围检查
- **权限验证**: 基于预约的访问控制
- **操作日志**: 完整的操作审计

### 数据安全
- **输入验证**: 防止SQL注入和XSS攻击
- **数据加密**: 敏感信息保护
- **备份机制**: 数据持久化存储

## 扩展性

### 水平扩展
- **微服务架构**: 可拆分为独立服务
- **负载均衡**: 支持多实例部署
- **数据库分离**: 支持读写分离

### 功能扩展
- **用户认证**: 可集成用户管理系统
- **通知功能**: 可添加邮件/短信通知
- **报表功能**: 可添加使用统计报表
- **硬件集成**: 可连接真实门禁设备

## 部署说明

### 开发环境
```bash
# 安装依赖
pip install -r requirements.txt

# 初始化数据库
python3 run.py --init-db

# 启动系统
python3 run.py
```

### 生产环境
```bash
# 使用生产配置
export FLASK_ENV=production

# 使用WSGI服务器
gunicorn -w 4 -b 0.0.0.0:5000 app:app
```

## 监控和维护

### 系统监控
- **日志记录**: 完整的操作日志
- **性能监控**: 响应时间统计
- **错误监控**: 异常情况告警

### 数据维护
- **定期备份**: 数据库备份策略
- **日志清理**: 定期清理历史日志
- **性能优化**: 数据库查询优化