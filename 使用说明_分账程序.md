# 分账程序使用说明

## 快速开始

### 方法一：一键启动（推荐）
```bash
./quick_start.sh
```

### 方法二：手动启动
```bash
# 1. 安装依赖
pip3 install --break-system-packages -r requirements_split_bill.txt

# 2. 初始化演示数据（可选）
python3 demo_split_bill.py

# 3. 启动程序
python3 split_bill_app.py
```

### 方法三：使用启动脚本
```bash
python3 start_split_bill.py
```

## 访问程序

启动成功后，打开浏览器访问：
- **本地访问**: http://localhost:5000
- **局域网访问**: http://你的IP地址:5000

## 基本使用流程

### 1. 添加用户
1. 点击导航栏的"用户管理"
2. 点击"添加用户"按钮
3. 填写用户信息（姓名必填）
4. 点击"添加"完成

### 2. 创建账单
1. 点击导航栏的"账单管理"
2. 点击"创建账单"按钮
3. 填写账单信息：
   - 账单标题（必填）
   - 总金额（必填）
   - 描述（可选）
   - 选择分账方式
   - 选择参与用户
4. 点击"创建账单"完成

### 3. 管理账单
1. 在账单列表中点击"查看详情"
2. 查看分账详情和支付状态
3. 点击"标记已支付"更新支付状态
4. 查看支付进度统计

## 分账方式说明

### 平均分账
- 所有参与者平均分摊总金额
- 适用于大部分AA制场景
- 例如：4人聚餐300元，每人75元

### 按比例分账
- 按指定比例分摊费用
- 适用于按消费比例分账的场景
- 例如：按消费金额比例分账

### 自定义分账
- 手动指定每人应付金额
- 适用于复杂的分账场景
- 例如：张三30元，李四20元

## 功能特点

- ✅ **多种分账方式** - 支持平均、按比例、自定义分账
- ✅ **用户管理** - 添加和管理参与用户
- ✅ **实时支付跟踪** - 标记支付状态，查看进度
- ✅ **数据持久化** - 自动保存所有记录
- ✅ **现代化界面** - 响应式设计，支持手机访问
- ✅ **导出功能** - 支持账单导出和分享

## 常见问题

### Q: 如何备份数据？
A: 直接复制 `split_bills.db` 文件即可备份所有数据。

### Q: 支持多少用户？
A: 理论上支持无限用户，建议单次分账不超过50人。

### Q: 数据安全吗？
A: 数据存储在本地，不会上传到任何服务器，完全私密。

### Q: 如何重置数据？
A: 删除 `split_bills.db` 文件，重新启动程序即可。

## 文件说明

- `split_bill_app.py` - 主程序文件
- `start_split_bill.py` - 启动脚本
- `demo_split_bill.py` - 演示数据脚本
- `quick_start.sh` - 一键启动脚本
- `requirements_split_bill.txt` - 依赖包列表
- `templates/` - 网页模板目录
- `split_bills.db` - 数据库文件（自动生成）

## 技术支持

如有问题，请检查：
1. Python版本是否为3.7或更高
2. 依赖包是否正确安装
3. 端口5000是否被占用
4. 防火墙是否阻止访问

---

**让分账变得简单！** 🎉